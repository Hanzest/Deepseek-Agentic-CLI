# Sub-Agent: codebase-summarizer

## Goal
Produce a comprehensive, structured Markdown summary of the entire Deepseek_Chatbot codebase, covering every source file, its purpose, dependencies, and architecture.

## Purpose
Context isolation — this is a pure documentation/analysis task that requires reading all source files and synthesizing a single reference document. Delegating keeps the main agent's context clean and allows the sub-agent to focus entirely on deep code analysis.

## Skills / Specialization
You are a specialist with deep expertise in the following areas:

- **Code analysis and documentation**
- **Software architecture documentation**
- **Markdown technical writing**
- **JavaScript/Node.js codebase auditing**

## Context
The project root is at `K:\Khanhs\Study\Projects\2026\Deepseek_Chatbot`. The codebase is an ESM JavaScript project. All files have been read into the agent's context. Key architectural facts:

- **main.js** (7 lines) — entry dispatch, imports `runChat()` from orchestrator.
- **lib/orchestrator.js** (297 lines) — orchestration brain: sets up OpenAI client, sliding context window, multi-turn loop, tool execution integration.
- **lib/tokenizer.js** (161 lines) — pure token estimation using tiktoken with heuristic fallback.
- **lib/cliInput.js** (58 lines) — I/O-bound user interaction: model selection, thinking toggle, generic ask().
- **lib/streamHandler.js** (62 lines) — streaming response parser returning { reasoning_content, content, tool_calls }.
- **helper.js** (5 lines) — barrel re-exports for backward compatibility.
- **tools/template.js** (69 lines) — `createToolHandler()` factory: adds logging, optional consent prompt, and try/catch wrapping.
- **tools/registry.js** (61 lines) — central tool map: `MANAGER_TOOLS` (full set incl. delegation) and `WORKER_TOOLS` (execution/inspection only, no delegation).
- **tools/callToolsInBatch.js** (183 lines) — batch executor: consent tools run sequentially, read-only tools run concurrently.
- **tools/executeTerminal.js** (75 lines) — shell execution via execSync, blocks .env/dangerous patterns.
- **tools/patchFile.js** (117 lines) — search-and-replace file editing with exact-match enforcement.
- **tools/readFileChunk.js** (119 lines) — line-range file reading with .env protection.
- **tools/getProjectTree.js** (170 lines) — gitignore-aware directory tree walk.
- **tools/searchWeb.js** (95 lines) — DuckDuckGo search via duck-duck-scrape.
- **tools/fetchUrl.js** (154 lines) — URL fetch → HTML → Markdown via cheerio + turndown.
- **tools/askUserPreferences.js** (111 lines) — multi-question preference prompts.
- **tools/delegateSubAgent.js** (253 lines) — generates structured Markdown prompt files in artifacts/ for sub-agent delegation.
- **docs/agents.md** (66 lines) — agent guidelines.
- **docs/system-architecture.md** (184 lines) — architecture document (already written, reference it).
- **README.md** (158 lines) — project readme.
- **package.json** — ES module, 6 dependencies.

The project uses a tiered consent model: `fetch_url` always requires user consent; `execute_terminal_command` and `patch_file` are auto-approved normally but require explicit `y/n` consent when `.env` is involved; all other tools run without consent. `.env` file reads are **auto-blocked** in `readFileChunk.js`. There are 5 tools available to WORKER_TOOLS and 6 to MANAGER_TOOLS.

## Deliverable
Write a file at `artifacts/codebase-summary.md` containing a structured summary with the following sections:

1. **Project Overview** — one-paragraph description of the project.
2. **File-by-File Analysis** — for each file in the project (excluding .gitignore, node_modules, .env, and the output file itself), provide:
   - Path
   - Purpose (1-2 sentences)
   - Key exports (functions/classes/constants)
   - Dependencies (imports from within and outside the project)
   - Notable design patterns (e.g., factory pattern, pure functions, consent model)
   - Lines of code (approximate)
3. **Architecture Diagram** — a textual ASCII flow diagram showing data flow from entry point through orchestration to tools.
4. **Security Model Summary** — how .env protection, consent gates, and dangerous-pattern blocking work.
5. **Dependency Map** — external npm packages and where they are used.
6. **DRY/SOLID Compliance Notes** — how the codebase follows single-responsibility, open/closed, and DRY principles.

---

## Instructions

1. Read this entire prompt carefully before starting.
2. Plan your approach before writing any code or making changes.
3. Produce exactly the deliverable described above.
4. When done, clearly state that the deliverable is complete.

---
*Generated by delegateSubAgent tool. Feed this entire prompt into a fresh conversation for true context isolation.*