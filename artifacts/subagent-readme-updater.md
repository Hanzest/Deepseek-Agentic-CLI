# Sub-Agent: readme-updater

## Goal
Update README.md to accurately reflect the current codebase implementation, including the modular architecture, all 10 tools, sub-agent system, and corrected project structure.

## Purpose
The current README.md is stale — it references old filenames (mainAPI.js, helper.js, modelTool.js) that no longer exist, lists only 6 tools when there are now 10, and doesn't mention the sub-agent delegation system. This needs an accurate rewrite.

## Skills / Specialization
You are a specialist with deep expertise in the following areas:

- **technical writing**
- **JavaScript project documentation**
- **accurate codebase analysis**

## Context
The project lives at K:\Khanhs\Study\Projects\2026\Deepseek_Chatbot. Key facts:

- Entry point: main.js (7 lines) imports runChat from lib/orchestrator.js
- lib/ contains: orchestrator.js (~319 lines), tokenizer.js (~161 lines), cliInput.js (~58 lines), streamHandler.js (~62 lines), subAgentLoop.js (~187 lines), subAgentTerminal.js (~120 lines)
- tools/ contains 10 tool files + registry.js + template.js + callToolsInBatch.js
- Three tool registries: WORKER_TOOLS (9 tools), SUBAGENT_TOOLS (9 tools, no consent), MANAGER_TOOLS (10 tools = WORKER_TOOLS + delegate_sub_agent)
- HYPERPARAMETERS: token_limit: 65535, token_multiplier: 1.5, stream: true, reasoning_effort: "high"
- Sub-agent system: delegate_sub_agent tool creates a prompt file in artifacts/, spawns an independent terminal window (subAgentTerminal.js), runs a separate model loop (subAgentLoop.js)
- Package dependencies: openai, dotenv, tiktoken, ignore, cheerio, turndown, duck-duck-scrape
- docs/agents.md and docs/system-architecture.md are accurate reference docs

The README must be written to be comprehensive but not overly verbose. Write it as a single complete file.

## Deliverable
Overwrite README.md in the project root with a complete, accurate rewrite that matches the current implementation. The new README must include:
1. Updated project structure reflecting main.js, lib/*, tools/*, docs/*, artifacts/
2. All 10 tools in the tools table (execute_terminal_command, patch_file, read_file_chunk, get_project_tree, search_web, fetch_url, ask_user_preferences, write_or_create_file, multi_file_search_string, delegate_sub_agent)
3. Entry point changed to `node main.js` (not mainAPI.js)
4. Updated dependencies matching package.json (openai, dotenv, tiktoken, ignore, cheerio, turndown, duck-duck-scrape)
5. Updated Code Overview section reflecting the modular architecture: main.js, lib/orchestrator.js, lib/tokenizer.js, lib/cliInput.js, lib/streamHandler.js, lib/subAgentLoop.js, lib/subAgentTerminal.js, tools/registry.js, tools/template.js
6. Updated configuration section referencing HYPERPARAMETERS in lib/orchestrator.js (token_limit: 65535)
7. Mention of the sub-agent delegation system and its architecture
8. Retain the existing security section and agent guidelines reference, but update them where stale

---

## Instructions

1. Read this entire prompt carefully before starting.
2. Plan your approach before writing any code or making changes.
3. Produce exactly the deliverable described above.
4. When done, clearly state that the deliverable is complete.

---
*Generated by delegateSubAgent tool. Feed this entire prompt into a fresh conversation for true context isolation.*